# เอกสารสรุประบบงาน: พอร์ทัลเทคโนโลยีดิจิทัลและธรรมาภิบาลข้อมูล (S.K.O. Digital Governance Portal)
> **สำนักงานสาธารณสุขจังหวัดสระแก้ว**  
> **เวอร์ชัน:** 3.0 (Unified 3-in-1 Governance Portal: Vite + Supabase + Gemini AI)  
> **ที่ตั้งโปรเจกต์:** `/Users/piyanatvichian/Documents/app_antigravity2.0/find-computer`

---

## 1. บทนำและภาพรวมของระบบรวม (Executive Summary)

ระบบนี้เป็น **One-Stop Digital Governance Portal (ศูนย์รวมระบบบริหารจัดการเทคโนโลยีดิจิทัล ความมั่นคงปลอดภัยไซเบอร์ และธรรมาภิบาลข้อมูล สสจ.สระแก้ว)** ออกแบบขึ้นเพื่อตอบโจทย์ผู้ดูแลระบบที่มีภารกิจหลัก 3 ด้าน โดยรวมศูนย์การทำงานไว้บนเว็บไซต์เดียว จัดการผ่านระบบยืนยันตัวตนเดียว (Single Sign-On) แต่แบ่งแยกฐานข้อมูลและการทำงานแต่ละภารกิจอย่างเป็นอิสระต่อกัน (Modular Independence):

```
┌────────────────────────────────────────────────────────────────────────────────────────┐
│               หน้าเว็บหลัก (Public Landing) -> แดชบอร์ดสรุปจัดหาคอมพิวเตอร์               │
└───────────────────────────────────────────┬────────────────────────────────────────────┘
                                            │ [ปุ่ม Login เจ้าหน้าที่]
                                            ▼
┌────────────────────────────────────────────────────────────────────────────────────────┐
│                        Admin Portal (สลับทำงานได้ 3 ภารกิจหลัก)                         │
├────────────────────────────┬─────────────────────────────┬─────────────────────────────┤
│   💻 1. จัดหาคอมพิวเตอร์    │    🛡️ 2. พรบ.ไซเบอร์ & CII    │    📋 3. ธรรมาภิบาล & PDPA  │
│   (IT Procurement)         │    (Cybersecurity Audit)    │    (Data Governance & ROPA) │
├────────────────────────────┼─────────────────────────────┼─────────────────────────────┤
│ • บันทึกคำขอครุภัณฑ์         │ • แบบประเมิน CII Self       │ • ทะเบียนกิจกรรมประมวลผล    │
│ • Gemini AI อ่าน PDF       │   Assessment (6 Domains)    │   ข้อมูล (ROPA 22 คอลัมน์)  │
│ • ตรวจคุณลักษณะ/ราคากลาง     │ • คำนวณคะแนน % ความสอดคล้อง │ • ฐานกฎหมาย ม.24 / ม.26     │
│ • จัดการประชุมและลงมติ      │ • คลังเอกสารนโยบาย/กรอบปฏิบัติ │ • บันทึกมาตรการความปลอดภัย  │
│ • พิมพ์แบบสรุป A4 แนวนอน     │ • แบบรายงานแจ้งเหตุภัยคุกคาม │ • แบบรายงานแจ้งเหตุข้อมูล   │
│ • พิมพ์รายงานการประชุมสารบรรณ │   ไซเบอร์ (CSIRT Incident)  │   รั่วไหล (Data Breach 72h) │
└────────────────────────────┴─────────────────────────────┴─────────────────────────────┘
                                            │
                                            ▼
┌────────────────────────────────────────────────────────────────────────────────────────┐
│                              ฐานข้อมูลคลาวด์ Supabase                                   │
│            (แยกตารางของแต่ละภารกิจชัดเจน ปลอดภัยสูง มี Row Level Security)             │
└────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. สถาปัตยกรรมทางเทคนิค (Tech Stack)

| ส่วนประกอบ | เทคโนโลยีที่เลือกใช้ | ข้อดีและเหตุผล |
|---|---|---|
| **Core Architecture** | **Vite 8 (Multi-Page Single App)** | เบา โหลดเร็ว ไม่ต้องมี Node.js Server รันตลอดเวลา สามารถ Deploy บน Static Host ได้ฟรี |
| **Frontend Language** | **Modern Vanilla JS (ES Modules)** | ตรงไปตรงมา โค้ดแบ่งโมดูลชัดเจนใน `src/modules/` ดูแลง่ายสำหรับผู้ใช้งานทั่วไป |
| **Design System** | **Custom CSS (`src/style.css`)** | ดีไซน์สะอาด รองรับ Responsive, Cards, Badges, Tabs และระบบพิมพ์เอกสารราชการ |
| **Typography** | **Google Fonts (Sarabun)** | มาตรฐานตัวอักษรราชการไทย ทั้งบนหน้าจอและงานพิมพ์ A4 |
| **Cloud Database** | **Supabase (PostgreSQL Cloud)** | ฐานข้อมูลความเร็วสูง เชื่อมต่อผ่าน Supabase Client SDK แยก Schema เป็นระเบียบ |
| **Authentication** | **Supabase Auth** | ล็อกอินด้วย Email/Password จัดการเซสชันปลอดภัย เข้าถึงได้ครบทั้ง 3 ภารกิจ |
| **AI Integration** | **Google Gemini 2.0 Flash REST API** | ใช้สกัดข้อมูลรายการครุภัณฑ์จากไฟล์ PDF/รูปภาพ พร้อมจับคู่ชื่อมาตรฐานอัตโนมัติ |
| **Document Export** | **Browser Print Engine & Word Blob** | พิมพ์ A4 ตามระเบียบงานสารบรรณ (ขอบ 2.5x3x2x2 ซม.) และส่งออกไฟล์ `.doc` |

---

## 3. โครงสร้างไฟล์ในโปรเจกต์ (File Structure)

```text
find-computer/
├── .env                              # ค่า Config (Supabase URL, Anon Key, Gemini Key)
├── .gitignore                        # รายการไฟล์ที่ไม่ขึ้น Git
├── admin.html                        # หน้าต่างระบบ Admin Portal (เข้าถึงได้ 3 ภารกิจ)
├── index.html                        # หน้าต่างแดชบอร์ดสาธารณะ (ระบบจัดหาคอมฯ)
├── package.json                      # การตั้งค่า Dependencies และ Scripts
├── vite.config.js                    # การตั้งค่า Vite Multi-page (index.html & admin.html)
├── SYSTEM_OVERVIEW.md                # เอกสารสรุประบบฉบับนี้
├── README.md                         # คู่มือเริ่มต้นใช้งานระบบ
├── legacy-gas/                       # โฟลเดอร์สำรองโค้ด Google Apps Script เดิม
├── scripts/
│   ├── inspect.js                    # สคริปต์ตรวจโครงสร้างชีตเดิม
│   ├── migrate.js                    # สคริปต์ย้ายข้อมูลเข้า Supabase
│   └── test-schema.js                # สคริปต์ทดสอบการเชื่อมต่อ Supabase
└── src/
    ├── admin.js                      # Controller หลักของระบบ Admin และแถบสลับ 3 โมดูล
    ├── dashboard.js                  # Controller หน้าแดชบอร์ดสาธารณะ (สลับหน้าจัดหาฯ และ ROPA สำหรับบุคลากร)
    ├── style.css                     # Stylesheet หลัก, Portal Navbar, Print Rules
    ├── lib/
    │   ├── supabase.js               # Supabase Client Instance
    │   ├── utils.js                  # ฟังก์ชันตัวช่วย (เลขไทย, วันที่ไทย, เงินบาท)
    │   ├── summary.js                # โมดูลสร้างแบบสรุปการจัดหาฯ (A4 แนวนอน)
    │   └── minutes.js                # โมดูลสร้างรายงานการประชุมสารบรรณ (A4 แนวตั้ง)
    └── modules/
        ├── cyber/
        │   ├── index.js              # โมดูล พรบ.ไซเบอร์ (CII Assessment, Docs, Incidents)
        │   └── data/
        │       ├── ciiData.json      # ชุดคำถามเกณฑ์ประเมิน CII ทั้งหมด (177 KB)
        │       └── sidebarData.json  # สารบัญประมวลแนวทางปฏิบัติด้านความมั่นคงปลอดภัย
        └── pdpa/
            ├── index.js              # โมดูล Admin PDPA & ROPA (Inventory, Form, Data Breach)
            ├── publicRopa.js         # โมดูล Self-Service ROPA หน้าเว็บสาธารณะ (ค้นหา/เพิ่ม/ทบทวนประจำปี)
            ├── ropaManager.js        # ตัวจัดการข้อมูล ROPA ซิงค์ Supabase + LocalStorage Fallback
            └── data/
                └── ropaConstants.js  # ฐานกฎหมาย ม.24/26 และข้อมูล ROPA เริ่มต้น
```

---

## 4. รายละเอียดฟังก์ชัน 3 ภารกิจหลัก

### 💻 ภารกิจที่ 1: ระบบจัดหาคอมพิวเตอร์ (IT Procurement)
* **บันทึกข้อมูลคำขอ (Dynamic Table):** เลือกรอบประชุม อำเภอ หน่วยงาน เพิ่มรายการได้ไม่จำกัด คำนวณราคากลางและคุณลักษณะ (`ตรงตามเกณฑ์`, `ไม่ตรงตามเกณฑ์`, `ไม่มีในเกณฑ์ราคากลาง`)
* **AI PDF Extraction:** แนบไฟล์ PDF/รูปภาพ ส่ง Gemini 2.0 Flash สกัดรายการครุภัณฑ์และจับคู่ชื่อมาตรฐานลงตารางทันที
* **จัดการรอบการประชุม:** สร้าง/ลบรอบประชุม บันทึกมติ (เห็นชอบ/ไม่เห็นชอบ/เงื่อนไข) และแก้ไขรายการ
* **เอกสารราชการ:** ออกแบบสรุปการจัดหา (A4 แนวนอน) และรายงานการประชุมทางการ (A4 แนวตั้งสารบรรณ) มี QR Code และบล็อกลายเซ็น

### 🛡️ ภารกิจที่ 2: ระบบ พรบ.ไซเบอร์ & CII (Cybersecurity Audit)
* **แบบประเมิน CII Self Assessment:**
  - รองรับข้อกำหนดกฎหมายไซเบอร์ ครอบคลุมทั้ง 6 หมวด (D1-D6)
  - ประเมินตามข้อกำหนด ม.43, ม.44 พร้อมระดับคะแนน 4 ระดับ (0: ยังไม่เริ่ม, 1: กำลังทำ, 2: ปฏิบัติประจำ, 3: ต่อเนื่อง)
  - คำนวณเปอร์เซ็นต์ความสอดคล้องภาพรวม (Compliance Score %) และแสดง Badge สรุปอัตโนมัติ
* **คลังประมวลแนวทางปฏิบัติ (Policy Library):**
  - แผนการตรวจสอบ (Audit Plan, Programme, Report)
  - การประเมินความเสี่ยง (Risk Policy, Risk Matrix, KRI)
  - แผนเผชิญเหตุไซเบอร์ (IR Plan Manual, Sectoral CSIRT)
* **รายงานแจ้งเหตุภัยคุกคามไซเบอร์ (Incident Report):**
  - ทะเบียนรับแจ้งเหตุการณ์ ระดับความรุนแรง (Critical, High, Medium, Low) และบันทึกมาตรการตอบสนอง

### 📋 ภารกิจที่ 3: ระบบธรรมาภิบาลข้อมูลและ PDPA (Data Governance & Privacy)
* **ทะเบียนกิจกรรมการประมวลผล (ROPA Dashboard):**
  - แสดงรายการกิจกรรมประมวลผลข้อมูลส่วนบุคคลตามมาตรา 39 แห่ง พรบ.คุ้มครองข้อมูลส่วนบุคคล พ.ศ. 2562
  - สรุปสถิติข้อมูลอ่อนไหว (Sensitive Data) และสถานะการทบทวนประจำปี (Annual Review Status)
* **ระบบ Self-Service สำหรับบุคลากรหน้าเว็บ (Public ROPA):**
  - เข้าถึงได้ผ่านหน้าแรก ไม่ต้องล็อกอินให้ยุ่งยาก
  - ค้นหากิจกรรม และกรองตามกลุ่มงาน/ปีงบประมาณ
  - เพิ่มกิจกรรมใหม่ หรือกดปุ่ม "แก้ไข / ทบทวน" ประจำปี พร้อมระบุชื่อผู้ทบทวนและปีงบประมาณ
* **ระบบจัดการสำหรับผู้ดูแลระบบ (Admin PDPA Manager):**
  - จัดการ เพิ่ม ลบ แก้ไข รายการ ROPA จากฝั่ง Admin
  - บันทึกการทบทวนข้อมูลและเชื่อมโยงกับฐานข้อมูล Supabase `ropa_records` แบบอัตโนมัติ
* **แบบรายงานการละเมิดข้อมูลส่วนบุคคล (Data Breach Notification):**
  - บันทึกการแจ้งเหตุละเมิดข้อมูลส่วนบุคคลตามข้อกำหนดแจ้งเตือน DPO ภายใน 72 ชั่วโมง


---

## 5. การเปิดใช้งานและการ Deploy

```bash
# ติดตั้ง dependencies
npm install

# รันโหมดทดสอบในเครื่อง (Development)
npm run dev

# บิลด์สำหรับนำไปขึ้นเว็บจริง (Production)
npm run build
```
เมื่อรัน `npm run build` จะได้โฟลเดอร์ `dist/` ซึ่งสามารถนำไปวางบน Hosting ทั่วไป หรือบริการฟรีอย่าง Cloudflare Pages / GitHub Pages / Vercel ได้ทันทีโดยไม่มีค่าใช้จ่ายเซิร์ฟเวอร์เพิ่มเติม
